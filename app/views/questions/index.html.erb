<h1>All Questions</h1>

<ul>
  <% @questions.each do |question| %>
    <li> <%= link_to "#{question.title}", question_path(question) %><br>
      <%= question.description %></li>
      <hr>
  <% end %>
</ul>
